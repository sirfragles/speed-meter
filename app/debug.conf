# SPDX-License-Identifier: Apache-2.0
#
# Extra configuration for bench debugging.
#
# NOTE: logging over J-Link RTT and the disabled UART console are now part of
# prj.conf (they apply to every build). This file is kept for build-command
# compatibility only - add any debug-only settings here.

