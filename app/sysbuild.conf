# Sysbuild configuration: build MCUboot as the bootloader and merge it with
# the application into a single flashable image (merged_*.hex).
#
# Build with: west build --sysbuild ...

SB_CONFIG_BOOTLOADER_MCUBOOT=y
SB_CONFIG_MERGED_HEX_FILES=y
